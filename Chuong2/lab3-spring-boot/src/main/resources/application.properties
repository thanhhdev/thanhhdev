spring.application.name=Lab3-SpringBoot

app.notification.channel=email
app.notification.from=no-reply@lab.com
app.notification.max-retries=3
app.notification.enabled=true

# Activate a profile to override the values above with application-{profile}.properties.
# Uncomment one of the lines below, or pass --spring.profiles.active=prod on the command line.
# spring.profiles.active=dev
# spring.profiles.active=prod
